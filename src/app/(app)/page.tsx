// ARCHIVO PRESERVADO PARA INTEGRIDAD - NO ES UNA RUTA ACTIVA
// Este archivo colisionaba con src/app/page.tsx (Orquestador de Raíz)
// Se ha desactivado la exportación para permitir que el servidor de Next.js compile correctamente.

'use client';

/*
// Código original preservado:
import { useEffect } from "react";
import { useRouter } from "next/navigation";

export default function AppRootPage() {
    const router = useRouter();
    useEffect(() => {
        router.push('/dashboard');
    }, [router]);
    return <p>Redirigiendo a Dashboard...</p>;
}
*/

export const dynamic = 'force-static';
